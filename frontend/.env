# Fix WebSocket connection failed for HMR (Hot Module Replacement)
# Uses window.location for WebSocket - resolves ws://localhost:3000/ws connection errors
WDS_SOCKET_PORT=0

# API base URL - change this to your Render backend URL for production
REACT_APP_API_URL=http://127.0.0.1:5000
